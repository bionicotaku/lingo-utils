# txmanager 测试环境变量配置示例
# 复制本文件为 .env 并填写实际的数据库连接信息

# ============================================================================
# 数据库配置 (必需)
# ============================================================================

# 测试数据库连接 URL
# 格式: postgresql://用户名:密码@主机:端口/数据库名?参数
#
# 示例:
# - Supabase: postgresql://postgres:密码@db.xxx.supabase.co:5432/postgres?sslmode=require
# - 本地数据库: postgresql://postgres:postgres@localhost:5432/txmanager_test?sslmode=disable
# - Docker: postgresql://postgres:postgres@localhost:5432/txmanager_test?sslmode=disable
#
# ⚠️ 警告:
# - 测试会创建和删除 test_txmanager 表
# - 建议使用独立的测试数据库，避免影响生产数据
# - 如果使用生产数据库，请确保测试表名不会冲突
TEST_DATABASE_URL=postgresql://postgres:your_password@your_host:5432/your_database?sslmode=require

# ============================================================================
# 可选配置
# ============================================================================

# 是否在测试后保留测试表 (默认: false)
# 设置为 true 可以在测试失败后检查数据
KEEP_TEST_TABLE=false

# 测试超时时间（秒）(默认: 120)
TEST_TIMEOUT=120

# 是否显示详细输出 (默认: true)
VERBOSE=true

# 是否生成 HTML 覆盖率报告 (默认: true)
GENERATE_HTML_REPORT=true
